/**
 * My Annotations 
 * 
 * Play With docker
 * 
 *                  ** COMMANDS **
 * ls | pwd
 * docker --help
 * docker ps | docker ps -a
 * docker stats
 * docker run --name primaryContainer -p 80:80 -d nginx
 * docker rm <nameContainer> | docker rm <nameContainer> -f
 * docker images
 * docker rmi <nameImage>
 * 
 * Instalando o Docker Linux e Windows
 * VMs (virtualBox, Azure, AWS)
 * Obs: Para Windows temos o Docker Desktop
 * 
 * commands used
 * docker Info
 * docker Exec
 * docker Stop, start
 * docker logs
 * docker inspect
 * docker pull
 * docker commit
 * docker tag
 * docerk login, logout
 * docker push
 * docker search
 * docker export, import 
 * docker save, load
 * 
 */